apiVersion: v1
kind: Secret
metadata:
  name: targeting-secret
  namespace: targeting
type: Opaque
stringData:
  # Ajuste host ao Service Postgres do targeting
  TARGETING_DATABASE_URL: postgres://targeting_user:targeting_password@postgres-targeting.targeting.svc.cluster.local:5432/targeting_db
