apiVersion: v1
kind: Service
metadata:
  name: flag-service
  namespace: flag
spec:
  selector:
    app: flag-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP

