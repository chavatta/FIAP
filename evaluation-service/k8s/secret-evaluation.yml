apiVersion: v1
kind: Secret
metadata:
  name: evaluation-secret
  namespace: evaluation
type: Opaque
stringData:
  # Chave criada via POST /admin/keys no auth-service (Bearer <MASTER_KEY>)
  SERVICE_API_KEY: substitua-pela-chave-de-api-valida
